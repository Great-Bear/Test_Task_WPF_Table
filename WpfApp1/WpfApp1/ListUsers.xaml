<Page x:Class="WpfApp1.ListUsers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local3="clr-namespace:WpfApp1.Classes"
      xmlns:fa="http://schemas.fontawesome.io/icons/" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ListUsers">
    <Page.Resources>
        <local3:DateConverter x:Key="DateConverter" />
        <local3:ListViewItemStyleSelector x:Key="myStyleSelector"/>

        <Style x:Key="MyHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Background" Value="LightBlue"/>
        </Style>

    </Page.Resources>


    <Grid Background="White" >
        <Grid Background="White" Margin="0,0,10,10">
            <ListView ItemContainerStyleSelector="{DynamicResource myStyleSelector}" SelectionMode="Extended"  GridViewColumnHeader.Click="GridViewColumnHeaderClickedHandler"  Name="usersList" ItemsSource="{ Binding  Users}"  Margin="80,10,0,0" SelectionChanged="usersList_SelectionChanged" >
                <!-- <ListView ItemContainerStyleSelector="{DynamicResource myStyleSelector}"  GridViewColumnHeader.Click="GridViewColumnHeaderClickedHandler"  Name="usersList" ItemsSource="{ Binding  Users}"  Margin="80,10,0,0" SelectionChanged="usersList_SelectionChanged" > -->
                <ListView.View >
                    <GridView ColumnHeaderContainerStyle="{StaticResource MyHeaderStyle}"  >
                        <GridViewColumn DisplayMemberBinding="{Binding Name}">Name</GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding SurName}" Width="100">Surname</GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding CreatedOn, Converter={StaticResource DateConverter}}">CreatedOn</GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding SubcriedTo, Converter={StaticResource DateConverter}}">CreatedOn</GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Decription}">Description</GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
            <Button Name="DeleteBtn" Content="Delete" Command="{ Binding DeleteCommand }"
                    CommandParameter="{ Binding ElementName=usersList, Path=SelectedItems }" HorizontalAlignment="Left" Margin="10,12,0,0" VerticalAlignment="Top" Height="33" Width="65" FontSize="14"/>
            <TextBox HorizontalAlignment="Left" Margin="10,50,0,0" TextWrapping="Wrap" Name="txtFilter" TextChanged="txtFilter_TextChanged" VerticalAlignment="Top" Width="65" Height="73"/>
            <!--  <fa:ImageAwesome Icon="Refresh" Spin="false" VerticalAlignment="Top" HorizontalAlignment="Left" Height="47" Width="53" Margin="10,128,0,0" /> -->
        </Grid>
    </Grid>
</Page>
